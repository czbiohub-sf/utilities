# Input directory
input_dir: # please fill in - example: "/path/to/input_dir"
fastq_regex: "(.*)_S[0-9]+(_L[0-9]+)?_R[12](_001)?.fastq.gz"
sample_id_replacement: "$1"

# Reference
reference: # please fill in - example: "reference.tar.gz"

# Output directory
publish_dir: # please fill in - example: "output/"

# Operational arguments
id: "auto_run"

# Nextflow input-output arguments
publish_dir: # please fill in - example: "output/"
# param_list: "my_params.yaml"
